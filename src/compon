import { motion } from "framer-motion";
import { memo } from "react";
import { Briefcase, Building2, Target, TrendingUp } from "lucide-react";

/* ------------------------------------------------------------------ */
/*  Premium stats strip — single glass container with icons           */
/* ------------------------------------------------------------------ */

type Stat = {
  value: string;
  label: string;
  suffix?: string;
  icon: React.ReactNode;
};

const STATS: Stat[] = [
  { value: "50+", label: "Projects Delivered", icon: <Briefcase className="h-5 w-5" /> },
  { value: "20+", label: "Industries", icon: <Building2 className="h-5 w-5" /> },
  { value: "98.6", label: "Accuracy", suffix: "%", icon: <Target className="h-5 w-5" /> },
  { value: "10x", label: "Operational Efficiency", icon: <TrendingUp className="h-5 w-5" /> },
];

const TRUSTED_BY = ["NUVANTICA", "PREDUIT", "NOVACORE", "VERTEX", "OMNILINE", "HELIOS"];

const HeroStats = memo(() => {
  return (
    <section className="relative w-full bg-black">
      <div className="mx-auto w-full max-w-[1400px] px-5 sm:px-8 lg:px-10">
        {/* Single premium glassmorphism container — 60-80px below hero */}
        <motion.div
          initial={{ opacity: 0, y: 24 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true, margin: "-60px" }}
          transition={{ duration: 0.6, ease: [0.22, 1, 0.36, 1] }}
          className="glass-card-strong relative mt-16 overflow-hidden rounded-3xl px-6 py-10 sm:px-10 lg:mt-20 lg:py-12"
        >
          {/* Subtle inner top glow */}
          <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_60%_80%_at_50%_0%,rgba(255,138,0,0.06),transparent_70%)]" />

          {/* Stats grid with thin dividers */}
          <div className="relative grid grid-cols-1 gap-y-8 sm:grid-cols-2 lg:grid-cols-4 lg:gap-y-0">
            {STATS.map((stat, i) => (
              <motion.div
                key={stat.label}
                initial={{ opacity: 0, y: 16 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true, margin: "-60px" }}
                transition={{ duration: 0.5, ease: [0.22, 1, 0.36, 1], delay: i * 0.08 }}
                className="relative flex flex-col items-center gap-3 px-4 text-center lg:items-start lg:text-left"
              >
                {/* Thin divider between items (desktop) */}
                {i > 0 && (
                  <span className="absolute left-0 top-1/2 hidden h-12 w-px -translate-y-1/2 bg-gradient-to-b from-transparent via-white/10 to-transparent lg:block" />
                )}

                <div className="flex h-11 w-11 items-center justify-center rounded-xl border border-orange-400/25 bg-orange-500/10 text-orange-300">
                  {stat.icon}
                </div>

                <div className="flex flex-col items-center lg:items-start">
                  <p className="text-[clamp(1.75rem,3.5vw,2.5rem)] font-bold leading-none tracking-[-0.02em] text-white">
                    {stat.value}
                    {stat.suffix && (
                      <span className="text-gradient-orange">{stat.suffix}</span>
                    )}
                  </p>
                  <p className="mt-2 text-[11px] font-medium uppercase tracking-[0.16em] text-white/45">
                    {stat.label}
                  </p>
                </div>
              </motion.div>
            ))}
          </div>
        </motion.div>

        {/* Trusted by */}
        <div className="py-12 lg:py-14">
          <motion.p
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            viewport={{ once: true }}
            transition={{ duration: 0.6, delay: 0.15 }}
            className="mb-8 text-center text-[11px] font-medium uppercase tracking-[0.28em] text-white/35"
          >
            Trusted by Innovative Enterprises
          </motion.p>

          <div className="flex flex-wrap items-center justify-center gap-x-10 gap-y-5 sm:gap-x-14">
            {TRUSTED_BY.map((name, i) => (
              <motion.span
                key={name}
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.45, delay: 0.25 + i * 0.06 }}
                className="select-none text-sm font-semibold tracking-[0.22em] text-white/25 grayscale transition-colors duration-300 hover:text-white/50 sm:text-base"
              >
                {name}
              </motion.span>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
});

HeroStats.displayName = "HeroStats";

export default HeroStats;